# Schema that defines a list of Rules in a Manifest
schema:
  allow_unknown: false
  type: list
  required: false
  schema:
    type: dict
    schema:
      id:
        type: string
        required: false